Placeholder so this empty folder ships with the mod. Parked-state JSON files
(parked_<steamid>.json) get written directly here at runtime. Safe to delete
this file manually on the server; the folder itself must exist for saves to
work, since Lua's file writer does not create missing parent directories.
